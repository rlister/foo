name: test
on: push
env:
  FOO: "test action"
jobs:
  hello:
    name: HelloJob
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Say Hello
      run: |
        ls -l
        env
