name: test
on: push
env:
  FOO: "test action"
jobs:
  hello:
    name: HelloJob
    runs-on: ubuntu-latest
    steps:
    - uses: actions/checkout@v2
    - name: Say Hello
      run: |
        ls -l
        env | grep GITHUB
        git rev-parse HEAD
        echo ${{ github.repository }}
        echo ${{ github.sha }}
        echo ${{ github.ref }}
        echo ${{ github.head_ref }}
